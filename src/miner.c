#include <stdlib.h>
#include <stdio.h>
#include "miner.h"

// Adds to pool of transactions ready to be mined
void addToPool(char *transaction) {
    printf("Adding to pool of transactions\n");

    return;
}

// Mines block from pool of transactions, returns pointer
// to string representing block
char *mineBlock() {
    printf("Consuming GPU market, please wait while I destroy the environment for the creation of 0 tangible value\n");
    // This will need to be multi threaded in the prototype
    return NULL;
}
